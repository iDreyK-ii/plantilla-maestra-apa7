/* Word is the main deliverable. Printing/PDF are separate, optional actions. */
function exportMenu(){
 if(!currentId)return openDownloads();
 dialog('Tu trabajo en Word',`<p class="lead">Descarga el contenido que has escrito en el editor como documento Word editable.</p><section class="export-word-panel"><h3>Word (.docx) — mi trabajo</h3><p>Incluye texto, portada, estilos, tablas, imágenes, citas y referencias. Es un archivo Word real, no una página web ni un MHTML.</p><button class="btn primary" onclick="closeModal('dialogModal');exportWord()">Word (.docx) — mi trabajo</button></section>${downloadChoicesHTML()}<p class="export-note">Word puede redistribuir las páginas. Si hay un índice, actualízalo en Word. Guarda también una Copia APA para conservar la biblioteca de fuentes al volver a la web.</p><div class="export-alternatives"><button class="btn" onclick="backupDocument()">Copia APA para seguir aquí</button><button class="btn" onclick="openOtherExports()">Otros formatos opcionales</button></div>`, '<button class="btn" onclick="closeModal(\'dialogModal\')">Volver al editor</button>');hydrateWordLinks($('dialogBody'));
}
function openOtherExports(){dialog('Otros formatos — opcionales',`<p class="lead">Estas herramientas son independientes de Word. No se ejecutan al descargar tu archivo .docx.</p><div class="quick"><button class="btn" onclick="closeModal('dialogModal');exportHTML()">HTML — documento limpio</button><button class="btn" onclick="closeModal('dialogModal');printDocument()">Imprimir / PDF — desde el navegador</button></div><p class="small">Para editar en Microsoft Word, utiliza «Word (.docx) — mi trabajo» en el menú anterior. No uses «Guardar página como MHTML» del navegador.</p>`, '<button class="btn" onclick="exportMenu()">← Volver a Word</button>');}
